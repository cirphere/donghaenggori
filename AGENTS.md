# 동행고리 AI — 리뷰어를 위한 컨텍스트

2026 AI+X 해커톤 Track 3 본선(8/19~21) 출품작. 병원동행 접수를 돕는 **사회복지사
Copilot**이다. 어르신이 전화로 말한 짧고 모호한 요청을 받아 접수카드를 만들고,
**확정은 사람이 한다.**

이 문서는 코드를 고치거나 리뷰할 때 **모르면 사고가 나는 것**만 적는다. 코드에서
읽을 수 있는 구조 설명은 넣지 않는다.

---

## 이 제품의 존재 이유 — 깨면 리팩터링이 아니라 사고다

1. **AI는 확정하지 않는다.** 후보와 근거까지만 낸다.
2. **상태는 3단계다** — `확인됨` / `추정` / `확인 필요`. 섞거나 2단계로 줄이지 않는다.
3. **발신번호로 대상자를 확정하지 않는다.** 프로필이 일치해도 사람이 확인한다.
4. **AI는 응급 여부를 판정하지 않는다.** 긴급 신호가 잡히면 접수카드를 만들다 말고
   사람에게 넘긴다 — 그래서 **긴급은 `card` 가 `null`이다.** 버그가 아니다.
5. **AI는 의료 판단을 하지 않는다.** 진단·처방·확진 표현이 코드에 있으면 안 된다.
6. **모든 확정·승인·수정은 감사 로그에 남는다.** 누가 무엇을 했는지 답할 수 없게
   되는 변경은 받아들이지 않는다.
7. **외부 LLM을 쓰지 않는다.** 어르신 발화가 기관 밖으로 나가지 않는 것이 발표
   논리이자 이 서비스의 성격이다. `ANTHROPIC_API_KEY`를 채우면 조용히 켜지므로
   기동 로그에 `AI 배치 — … 외부 LLM 미사용`을 찍는다.

`donghaenggori/web/api.py`의 `Policy` 모델이 1·5를 **리터럴로 고정**해 응답마다
싣는다. 원칙을 주석이 아니라 계약으로 만든 것이니 지우지 말 것.

---

## 무인증으로 열려 있는 경로 — 여기가 제일 위험하다

로그인 없이 부를 수 있는 곳이 셋이다. **하나라도 저장된 값을 더 돌려주면 그 순간
조회 API가 된다.**

| 경로 | 지키는 방법 |
|---|---|
| `POST /api/guardian/intakes` | `GuardianIntakeOut` **화이트리스트**. 부른 사람이 적어 보낸 것만 |
| `POST /api/guardian/lookup` | 신청번호 + 보호자 연락처 **둘 다** 맞아야 함. 5회 실패 시 차단 |
| `POST /api/voice/*` | ClawOps 웹훅 서명(HMAC-SHA256) |

실제로 사고가 났던 적이 있다. 직원용 응답을 그대로 돌려줘서, 토큰 없이 남의 번호만
넣으면 이름·나이·주소·보호자 전화·거동·**독거 여부**·장기요양등급·진료이력이 전부
200으로 나왔다.

**`response_model` 필터링을 믿지 않는다.** 모델에 필드가 하나 늘거나 `extra` 설정이
바뀌면 조용히 다시 샌다. 명시적으로 골라 담는다.

`tests/test_guardian_privacy.py`(39건)가 이 경계를 지킨다. **여기가 빨개지면 테스트를
고치지 말고 원인을 찾을 것.**

### 조회에서 내보내지 않는 것

`/api/guardian/lookup`은 **확정 전에는 병원·일정을 안 준다.** 확정 전 값은 AI가 고른
후보이고, 그 후보는 **어르신의 과거 이력에서 나온다.** 보호자가 적지 않은 병원 이름이
뜨면 우리가 그 이력을 알려 준 것이다.

`target`도 안 준다. 보호자가 "김순자"로 신청했는데 백엔드가 보호자 번호로 역조회해
"박순자"를 찾으면, **그 번호로 등록된 다른 어르신의 이름을 알려 준 셈**이 된다.

---

## 기존 흐름이 못 하는 요청 (`donghaenggori/core/requesttype.py`)

접수 발화가 전부 '병원동행'인 것은 아니다. 어디로 갈지 몰라 묻는 전화, 사람을 보내
달라는 전화가 온다. 그런 요청에 **과거 단골을 후보로 내미는 것이 이 코드에서 제일
흔한 지어내기였다** — 어디로 갈지 몰라 전화한 사람에게 지난번 병원을 확정해 준다.

의도 분류(`병원동행`/`약국`/…)와 **다른 축**이다. 의도가 `병원동행`인 접수를 다시
다섯으로 가른다: `기존재방문` / `신규병원탐색` / `진료과기반탐색` / `돌봄인력요청` /
`기타불분명`.

- **`기존재방문`이면 지금까지의 흐름 그대로다.** 나머지 넷에서만 동작이 달라진다
- **애매하면 `기존재방문`이다.** 탐색·인력 신호가 뚜렷할 때만 새 유형으로 본다 —
  잘못 보내면 접수카드가 아예 안 나온다
- **긴급이 먼저다.** 유형 분기는 긴급 판정 뒤에 온다. 새 유형이 안전 동작을 가리면 안 된다
- 새 유형에서는 **병원 후보 산출과 진료과 추론을 끈다.** 증상 사전이 고른 진료과
  ("허리가 아픈데" → 정형외과)도 카드에 싣지 않는다 — 그 값이 조회 조건·배차 기준이
  되어 사실처럼 굳는다. 추정했다는 사실만 근거에 남긴다
- 병원 후보는 `services/hospital_lookup.py`로 **심평원에서 조회된 것만** 낸다. 키가
  없거나 조회가 실패하면 **후보를 만들지 않고 그 사유를 문장으로 남긴다** — 빈 칸이
  "이 지역에 병원이 없다"로 읽히면 안 된다
- **`돌봄인력요청`에 자동 배정 로직을 만들지 않는다.** 인력 배치 데이터가 없다
- 통화 안내도 다르다(`web/voice.py::_receipt`). 화면에는 '확인 필요' 배지가 있지만
  **통화에는 없다** — "말씀하신 날짜로 접수했습니다"가 어르신에게는 확정으로 들린다

새 유형 카드에는 `request`(요청 내용) 칸이 서고 그 칸이 `gate.BLOCKING`에 들어 있다.
**이 칸을 빼면 새 유형이 조용히 확정으로 통과한다** — 병원·날짜 칸을 세우지 않았으니
막을 것이 없어져서, 유형을 가르는 일이 오히려 게이트를 푸는 결과가 된다.

`tests/test_request_type.py`(75건)가 이 경계를 지킨다.

---

## 이동지원·보호자는 출처가 둘이다 (`donghaenggori/core/mobility.py`)

성능평가로 드러난 것 — 카드의 `need_level`(지원 수준)과 `guardian`(연락처)은 **발화가
아니라 케어 프로필에서 온다.** 어르신이 "나 혼자 갈 수 있어" 라고 말해도 카드는
프로필 등급으로 "휠체어·부축 동행" 을 내놓았고, 그 말은 카드에 남지 않았다.

**합치지 않고 나란히 둔다.**

| | 출처 | 무엇 |
|---|---|---|
| `need_level` | 프로필 | 지원 수준(휠체어·부축 등) — 배차에 쓴다 |
| `mobility_need` | 발화 | 이번 통화에서 필요하다고 했는가 |
| `guardian` | 프로필 | 연락처 — 못 만났을 때 전화할 곳 |
| `guardian_mentioned` | 발화 | 통화에서 들은 보호자 이야기 |

- **mobility.py 는 프로필·이력·DB 를 보지 않는다.** 입력은 발화 텍스트뿐이다. 한 번
  섞이면 "발화에서 뽑았는지 프로필에서 왔는지" 를 다시 구분할 수 없다
- **`가족지원있음`을 `명시적_불필요`와 갈라 둔다.** 둘 다 '불필요·확정' 이지만 근거가
  다르다 — 가족이 못 오게 되면 바로 필요해진다. 복지사가 그 차이를 알아야 한다
- `신호없음`이면 '확인 필요' 다. **그때 프로필의 지원 수준을 이 칸에 끌어오지 않는다**
- 규칙 기반이다. 근거는 원문 문구 그대로 남긴다. 방언 표현은 팀 사전
  (`docs/eval/전남방언_매핑사전.xlsx`)의 항목을 따랐다
- **held-out 문장으로 재보면 44% 만 잡는다.** 문자열 포함 매칭이라 같은 뜻을 다른
  말로 하면 놓친다("다리가 시원찮", "혼자서도 넉넉히 갈 수 있"). 사전을 넓히는
  대신 **못 잡으면 통화에서 되묻는다**(followup 의 ASK_ORDER 에 mobility_need 가
  있다). 놓친 것이 전부 '확인 필요' 로 떨어지는 방향이라 잘못된 확정은 없다
- 카드 항목 키는 `mobility_need` 다. **`mobility` 로 두지 않는다** — 카드에 이미
  거동 상태(프로필의 '휠체어 사용' 등)가 그 이름을 쓰고 있어서, 겹치면 이 분리가
  고치려던 문제를 그대로 다시 만든다

`tests/test_mobility.py`(46건)가 이 경계를 지킨다.

---

## 통화 중 후속질문 (`donghaenggori/core/followup.py`)

확인 필요로 남은 칸을 **어르신이 아직 통화 중일 때** 되묻는다. 동행 정보의 기본
여섯(병원·방문일·방문시각·진료과·이동지원·대상자)을 한 번에 하나씩, 통화당 최대 6회다
(`CLAWOPS_FOLLOWUP_MAX=0` 이면 꺼진다. 기본 5회).

**되묻는 목록은 `gate.BLOCKING` 과 다르다.** 막는 기준은 "일정이 성립하는가" 이고
묻는 기준은 "빠지면 복지사가 다시 전화해야 하는가" 다. 진료과는 막지 않지만 묻고,
시각은 말한 적 없어도 묻는다. 되물어 못 얻으면 '확인 필요' 로 남을 뿐이고 **확정
정책은 그대로다** — 이 둘을 한 목록으로 합치면 진료과가 확정을 막게 되어 "진료과를
몰라도 동행은 나간다" 가 깨진다.

**'확인됨' 이 아니면 묻는다 — 추정도 묻는다.** 처음에는 '확인 필요' 만 물었는데,
그러면 단골 병원이 빠져나간다(이력 2회면 '추정' 이고 추정은 안 물었다). **어르신이
말한 적 없는 병원으로 동행을 나가는 것**이 이 저장소가 제일 경계하는 사고인데,
되묻는 기능이 정작 그걸 건너뛰고 있었다.

반대로 **아는 것은 묻지 않는다.** 발신번호로 확인된 이름을 다시 확인하는 것은
#105 에서 뺀 질문이고(답이 카드를 바꾸지 않았다), 넣으면 정보가 다 있는 통화까지
턴이 하나 늘어난다. 이름을 모르는 접수는 대상자가 '확인 필요' 라서 자동으로 물어진다.

- **질문을 새로 만들지 않는다.** `gate.blockers[].question` 을 그대로 쓴다 — 두 곳에서
  각자 만들면 화면이 띄운 질문과 통화가 묻는 질문이 갈라진다
- **사람 연결 신호를 재추출보다 먼저 본다.** "됐어요"·"사람 바꿔줘"가 나왔는데 답에서
  병원 이름을 뽑고 있으면, 이 통화에서 제일 하면 안 되는 일(한 번 더 캐묻기)을 한다
- **접수를 먼저 저장하고 되묻는다.** 되묻는 중에 끊겨도 접수가 사라지면 안 된다
- **`target` 은 묻되 값을 채우지 않는다.** 8kHz 전사 이름은 어떤 경로로도 '확인됨'이
  되지 않고, '추정' 을 주면 게이트가 풀려 불변조건 3(발신번호로 대상자를 확정하지
  않는다)이 무너진다. 들은 성함은 후속질문 기록에 남기고 확정은 사람이 한다.
  미등록 번호는 통화 앞에서 이미 성함을 받으므로 다시 묻지 않는다
- **신규 유형(requesttype.STAFF_HANDLED)에는 적용하지 않는다.** 되물어 풀리는 종류가
  아니다
- 답을 못 얻으면 그 칸은 '확인 필요' 그대로 둔다. **우리가 채우지 않는다**

`db.apply_followup` 은 `verify_card_field` 와 **일부러 갈라 뒀다.** 저쪽은 사람이
확인한 것(`항목확인` · `verified_by`)이고 이쪽은 자동 전사(`후속질문` · 행위자
`전화 시스템`)다. 합치면 사고가 났을 때 누가 확인했는지 답할 수 없다.

`tests/test_followup.py`(121건)가 이 경계를 지킨다.

---

## 확정 게이트 (`donghaenggori/core/gate.py`)

확인 필요가 남으면 confirm이 **409**로 막힌다. 422가 아니다 — 요청이 틀린 게 아니라
지금 상태에서 못 하는 것이다.

- **막는 기준은 "일정을 세우는 데 필요한가"** — 대상자·병원·방문일은 막고 진료과는 안 막는다
- **`추정`은 막지 않는다.** 막으면 거의 모든 카드가 걸려 3단계가 2단계로 무너진다
- **시각은 `value`가 아니라 `spoken`으로 가른다.** 모호한 "3시"는 해석 실패로
  `value=None`이라, `value` 기준으로 짜면 정작 막아야 할 것이 통과한다
- `verify`가 게이트를 푸는 **유일한 경로**다. 없으면 미등록 접수가 영구 잠긴다
- `acknowledge:true`로 넘어가면 감사 로그에 `미확인 확정`과 **사유**가 남는다

**`/verify`는 "통화로 확인함"을 뜻한다.** 화면에서 값을 고른 것과 다르다. 그 구분이
무너지면 사고가 났을 때 누가 실제로 확인했는지 답할 수 없다.

---

## 최근에 고친 것 — 되돌리지 말 것

| | 무엇이 문제였나 |
|---|---|
| 긴급 확정 차단 | 긴급은 카드가 없어 blockers가 비고 `allowed=true`로 통과했다. 확정하면 status가 덮여 **긴급 표시가 사라진다** |
| 확정 멱등 | 부를 때마다 감사 로그를 남겨 두 번 누르면 두 줄이 쌓였다. 값이 달라졌을 때만 남긴다 |
| 사후기록 생성 권한 | 승인은 권한을 보는데 생성은 인증만 봤다 |
| 거절 기록 | `approved=0`이 "아직 안 봤다"와 "반영 안 하기로 했다"를 같이 뜻해 거절이 아무 흔적도 안 남겼다. `reviewed` 컬럼으로 갈랐다 |

---

## 데이터 계층에서 자주 틀리는 것

- **컬럼을 명시적으로 골라 담는 함수가 있다.** `get_profile`이 그렇다 — 스키마에
  컬럼을 더해도 거기 안 넣으면 안 나온다. 실제로 `address`를 빠뜨렸다
- `list_intakes`가 `profiles`를 조인해 **나이·지역만** 끌어온다. 건강 정보는 안
  가져온다 — 목록에 실리면 화면을 여는 것 자체가 개인정보 열람이 된다
- `_ORDER`의 컬럼은 `i.`로 못박혀 있다. `profiles`에도 `id`가 있어 조인에서 모호해진다
- 스키마 변경은 `SCHEMA`(새 DB)와 `_ADDED_COLUMNS`(기존 DB) **둘 다** 고쳐야 한다
- **`docker compose down -v` 금지.** `db-data` 볼륨이 날아가 계정까지 사라진다

---

## 프론트엔드 — 저장소가 둘이다

| | |
|---|---|
| `frontend/public/` (이 저장소) | 바닐라 JS. `/`(보호자 웹) `/portal`(보호자 포털) `/staff` `/console` |
| `dlghskgmll/donghaengori-ai` | **팀원의 Next.js 관리자 콘솔 — 발표용 최종 UI** |

**팀원 저장소가 본선에 쓸 화면이다.** 그쪽 `FRONTEND-INTEGRATION-HANDOFF.md`가
디자인 가드레일(고정 치수·브랜드 색 `#F94704`·금지 사항)을 명시하고 있으니 화면을
고치기 전에 읽을 것.

이 저장소의 화면들은 **대체 경로**로 남겨 둔다. Next가 문제를 일으키면 그쪽으로 간다.

브라우저는 우리 백엔드를 직접 부르지 않는다 — 팀원 앱의 `app/api/v1/*` proxy를 거친다.

### 이 저장소 화면의 규칙

- 토큰은 **`sessionStorage`**. `localStorage` 아니다 — 복지관 공용 PC다
- **외부에서 아무것도 받지 않는다.** 글꼴(Pretendard)도 `frontend/public/fonts/`에
  자체 호스팅한다. 페이지에서 도는 스크립트가 늘면 토큰을 읽을 수 있는 주체가 는다
- 권한은 서버가 준 `permissions` 목록으로 가린다. **역할 이름을 JS에 박지 않는다** —
  권한표가 두 곳이 되면 어긋난다
- **화면이 가리는 것은 안내일 뿐이고 경계는 서버의 403이다**
- 상태 라벨(`접수 대기`→"접수 전")은 **화면에서만** 옮긴다. DB 값을 바꾸면
  파이프라인·게이트·통계가 전부 딸려 온다

---

## 검증

```bash
PYTHONPATH=. python tests/test_gate.py            # 39
PYTHONPATH=. python tests/test_guardian_privacy.py # 39  ← 개인정보 경계
PYTHONPATH=. python tests/test_voice.py           # 71
PYTHONPATH=. python tests/test_file3_cases.py     # 26  ← 제출 문서 케이스
PYTHONPATH=. python tests/test_identity_extract.py # 39
PYTHONPATH=. python tests/test_request_type.py    # 75  ← 새 요청 유형 경계
PYTHONPATH=. python tests/test_followup.py       # 131 ← 통화 중 후속질문
PYTHONPATH=. python tests/test_mobility.py       # 46  ← 이동지원·보호자 출처 분리
ruff check .
```

`tests/demo_scenario.py`는 **17/17**이 나와야 한다. API만 두드리므로 **화면이
멀쩡하다는 뜻은 아니다.**

`tests/metrics_*.py`는 제출 문서(파일1)의 성능 지표를 잰다. CI에 안 걸어 뒀다 —
목표에 턱걸이(0.860 / 0.950)라 데이터가 조금만 바뀌어도 빨개진다.

**브라우저로 띄워야만 나오는 버그가 반복해서 나왔다.** UI를 고쳤으면 실제로 열어
볼 것.

---

## 리뷰할 때 특히 봐 주면 좋은 것

- 무인증 경로가 저장된 값을 더 돌려주지 않는가
- 감사 로그가 빠지거나 중복되는 경로가 없는가
- 긴급(`card=null`) 분기를 빠뜨린 곳이 없는가
- 게이트 판정을 화면이 흉내 내는 곳이 없는가 (서버 `gate`를 그대로 써야 한다)
- 스키마에 컬럼을 더했는데 읽는 쪽에 안 넣은 곳이 없는가
